[project]
name = "ip6-agents"
version = "0.2.0"
description = "Multi-agent network administration with Pydantic AI agent delegation"
requires-python = ">=3.13"
dependencies = [
    "pydantic-ai-slim[openai,logfire,mcp]",
    "python-dotenv",
    "httpx",
    "logfire[httpx]>=4.29.0",
    "pyyaml",
    "netmiko",
    "fastmcp",
]
